"""Deterministic, LLM-free remediation engine (the product).

Parsing, filtering, dedupe, and Maven-aware version comparison live here. No LLM,
no network — adapters (CLI, MCP) wrap these functions.
"""
